# ============================================================
# .env.example – Konfigurationsvorlage für digitize_board.py
# ------------------------------------------------------------
# Kopiere diese Datei zu ".env" und trage deine Werte ein:
#   cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# PFLICHTFELD – ohne diesen Key startet das Skript nicht
# ------------------------------------------------------------
# Deinen API-Key findest du unter: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-DEIN-API-KEY-HIER

# ------------------------------------------------------------
# OPTIONALE FELDER (Standardwerte sind bereits eingetragen)
# ------------------------------------------------------------

# Standard-Modell für Vision-Analyse
# Vision-fähige Modelle auf OpenRouter: https://openrouter.ai/models?modalities=image
DEFAULT_MODEL=google/gemini-2.0-flash

# Fallback-Modell bei Fehler des Standard-Modells (besser bei Handschriften)
FALLBACK_MODEL=anthropic/claude-sonnet-4-5

# Ausgabe-Ordner für die generierten Markdown-Dateien
OUTPUT_DIR=./output

# Maximale Anzahl an Tokens pro API-Antwort
# Erhöhen bei sehr großen/vollen Boards (z.B. 8000)
MAX_TOKENS=4000
